import { PhaserGame } from "./PhaserGame";

/**
 * React component that wraps the Phaser game.
 */
function App() {
    return (
        <div id="app">
            <PhaserGame />
        </div>
    );
}

export default App;
